import {
  BlockPayload,
} from "../define";
import { GameContext } from "../model";

export function doBlockPayload(
  ctx: GameContext,
  blockPayload: BlockPayload
): GameContext {
  return ctx;
}

import {
  BlockPayload,
} from "../define";
import { GameContext, GameState } from "../model";

export function doBlockPayload(
  ctx: GameState,
  blockPayload: BlockPayload
): GameState {
  return ctx;
}

package main

import (
	"src/aerospike_db"
)

func main() {
	aerospike_db.Test()
	// address := "http://localhost"
	// port := 3000
	// client, err := aerospike.NewClient(address, port)
	// if err != nil {
	// 	log.Fatal(err)
	// }
	// defer client.Close()
	// println("end")
}

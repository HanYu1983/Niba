version: '3.8'
services:
  app:
    image: node:18-alpine
    working_dir: /app
    command: sh -c "yarn install && yarn start"
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development
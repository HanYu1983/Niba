# container
    create_container.bat
    enter_container.bat

# create project
    lein new game

# run project
    lein run

# run test
    lein test
    
# run repl
    lein repl
    (use 'game.core :reload)
    (macroexpand-1 '(code))

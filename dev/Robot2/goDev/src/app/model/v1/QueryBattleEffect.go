package v1

import (
	"app/tool/protocol"
)

type Effect struct {
}

func QueryBattleEffect(model model, robot protocol.Robot, transform string, weapon protocol.Weapon, targetRobot protocol.Robot) (int, error) {

	return 0, nil
}

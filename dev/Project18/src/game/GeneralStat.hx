package game;

/**
 * GDD：每名武將固定暴露四個數值維度，供征戰、計略結算與 UI 展示共用同一語彙。
 */
enum GeneralStat {
    /** 統御：編隊、潰敗門檻或隊伍加成類規剘引用。 */
    Command;

    /** 勇武：白刃、強攻類規剘引用。 */
    Might;

    /** 智謀：計略對抗、看破類規剘引用。 */
    Wit;

    /** 政理：城池／資源類結算或後勤類規剘引用。 */
    Stewardship;
}
